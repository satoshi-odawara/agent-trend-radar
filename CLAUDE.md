# agent-radar

AIエージェントツールが実際のソフトウェア開発で「どう使われているか」を、
公開リポジトリの実態から定量的にサーベイし、OSSとして公開するプロジェクト。
単なる人気度(stars等)ではなく、品質・セキュリティ・運用の実践知がどう
現れているかをエビデンスとして示すことを狙う。

## 目的・差別化
- 感想ではなく「実プロジェクトで実際どう使われているか」をエビデンスで示す
- 資産はデータインサイト(実態データ)。実装方式は固定せず、都度最適な形に
  見直す

## 開発環境
- パッケージ管理: uv(pip/poetryは使わない)
- Python: 3.12+
- コマンド例:
  - 依存追加: `uv add <package>`
  - 実行: `uv run <script>`
  - lockファイルは必ずコミットする

## アーキテクチャ
- データ収集: GitHub API を直接叩く(収集処理自体にLLM/agentフレームワーク
  は使わない。理由: コスト削減と、エビデンスの信頼性のため収集は決定的に行う)
- 分析: MVPでは「特定ファイルの有無」の機械的チェックに加え、CLAUDE.md/
  AGENTS.mdについてはキーワード一致・見出し数・文字数などルールベースの
  構造分析までを対象とする。LLMによる要約・解釈は行わない(理由は上記の
  収集の決定性と同じ)
- 保存: SQLite(ファイルベース、DBコスト0円)
- 自動実行: GitHub Actions(週次cron)
- LLM利用: 要約・記事生成、およびCLAUDE.md/AGENTS.mdの内容分析(ルール
  ベース分析の後継、reports/agent_doc_analysis_validation.md参照)に限定
  (低コスト優先のモデルを使う)。MVPでは未使用でよい

## 対象プロジェクト・チェック項目
詳細な分析対象リポジトリ・チェック項目は @SPEC.md を参照。

## 対象の見直し
- SPEC.mdの対象リポジトリ・チェック項目は固定リストではない
- 気づいたタイミングで追記・入れ替えを行う。頻度を厳密には定めない
  (月1回程度を目安とするが、これに縛られない)
- 見直しの判断基準や頻度は、運用しながら適宜この節を更新する

## スコープ方針
- MVPは「実プロジェクトのファイル存在分析」と「CLAUDE.md/AGENTS.mdの
  ルールベース構造分析」に一本化する(LLMによる内容の要約・解釈は含めない)

## 制約
- 低コスト運用必須。有料SaaS・高額API課金は避ける

## 進め方
- Issueで作業単位を管理する
- 実装はClaude Codeに委譲する。方針検討・データ解釈はClaudeチャット
  (Projects)側で行う



---

  LLMによる一般的なコーディングミスを減らすための行動指針です。

**トレードオフ:** これらの指針は、スピードよりも「慎重さ」を優先しています。些細なタスクについては、状況に応じて判断してください。

## 1. コードを書く前に考える

**仮定で進めない。疑問をうやむやにしない。トレードオフを明確にする。**

実装する前に：

* 自分の仮定を明示してください。不確実な点があれば質問してください。
* 複数の解釈ができる場合は、勝手に判断せず、それらを提示してください。
* よりシンプルなアプローチがある場合は、それを伝えてください。正当な理由があれば、提案に異議を唱えて（プッシュバックして）ください。
* 不明瞭な点がある場合は、手を止めてください。何が混混乱の原因なのかを特定し、質問してください。

## 2. シンプルさ最優先

**問題を解決する最小限のコード。推測による実装はしない。**

* 要求されていない機能は追加しない。
* 1回しか使わないコードに対して、抽象化を行わない。
* 要求されていない「柔軟性」や「設定変更のしやすさ（設定可能性）」を持たせない。
* 起こり得ないシナリオに対するエラーハンドリングは書かない。
* 50行で済むところを200行書いてしまった場合は、書き直してください。

「シニアエンジニアが見たら『複雑すぎる』と言うだろうか？」と自問してください。もしそうなら、シンプルにしてください。

## 3. 外科手術のようなピンポイントの変更

**必要な部分だけを触る。自分が触った部分の始末だけをつける。**

既存のコードを編集する時：

* 隣接するコード、コメント、フォーマットなどを「改善」しようとしないでください。
* 壊れていないものをリファクタリングしないでください。
* 自分の好みとは違っていても、既存のスタイルに合わせてください。
* 関係のないデッドコード（使われていないコード）に気づいた場合は、指摘するにとどめ、削除はしないでください。

自分の変更によって使われなくなったコード（オーファン）が生じた時：

* **自分の**変更によって不必要になったインポート、変数、関数は削除してください。
* 指示がない限り、以前から存在していたデッドコードは削除しないでください。

評価基準：変更されたすべての行が、ユーザーの要求に直接結びついていること。

## 4. ゴール駆動の実行

**成功基準を定義する。検証されるまでループを回す。**

タスクを検証可能なゴールに変換してください：

* 「バリデーションを追加する」 → 「無効な入力に対するテストを書き、それをパスさせる」
* 「バグを修正する」 → 「バグを再現するテストを書き、それをパスさせる」
* 「Xをリファクタリングする」 → 「前後のテストがパスすることを確認する」
