# Development_plan.md

agent-trend-radar の MVP開発計画。詳細な仕様は @SPEC.md、プロジェクト方針は
@CLAUDE.md を参照。本ドキュメントは「何を・どの順で作るか」を定める。

## 目的

「必要最低限のMVP」を最短で動かし、実データに触れながら以下を確認する。

- チェック項目(SPEC.md記載の6項目)が実際に意味のある差を見せるか
- リポジトリ×項目のマトリクスとして見たときに、読み取りやすい形は何か
- 運用(週次実行・対象リポジトリの見直し)に乗せる前に、手動実行でどこが
  面倒か・どこを直すべきかを把握する

MVPの目的は「完成品」ではなく「使用感・活用イメージを固めるための土台」。
そのため自動化(GitHub Actions)やLLM連携は意図的に後回しにする
(CLAUDE.mdのスコープ方針と一致)。

## MVPのゴール(Definition of Done)

以下が手元で一気通貫で動けば MVP 完了とする。

1. `uv run` で実行できるスクリプトが1本あり、
2. 手動選定した10〜20リポジトリに対し GitHub API を叩いて SPEC.md の
   6チェック項目を判定し、
3. 結果を SQLite(`repo_checks`テーブル)に保存し、
4. 保存結果を人間が読める形(Markdown表 or CSV)で確認できる。

GitHub Actions化・LLM要約・Webでの公開は MVP の範囲外(Phase 7以降)。

## スコープ

**やる(MVP)**
- 対象リポジトリの手動選定(SPEC.mdのTBDを埋める)
- GitHub API(REST, Contents API相当)でのファイル/ディレクトリ存在チェック
- SPEC.md記載の6項目の判定ロジック
- SQLiteへの保存
- 結果を見るための簡易出力(Markdown表 / CSV)

**やらない(MVP後に検討)**
- GitHub Actionsによる週次自動実行
- LLMによる要約・記事生成(2026-09-08 方針転換によりこのリポジトリの
  スコープ外に確定。データ考察はClaude Projectの専用チャットスペースで
  行う。詳細はIssue.md #12参照)
- Web UI / 公開ダッシュボード
- 対象リポジトリの自動選定

## オープンな論点(進めながら決める前提で仮決定)

- `has_observability_dep` はファイル存在チェックではなく依存内容の確認が
  必要(SPEC.mdの対象パス例が「依存にLangSmith等」となっているため)。
  MVPでは `pyproject.toml` / `package.json` / `requirements.txt` 等の
  マニフェストファイルを1回読み込み、既知パッケージ名(langsmith, langfuse,
  langchain-tracing等)の文字列一致を見る程度の軽量実装に留める。
  複雑なパーサは作らない。
- 対象リポジトリの最終選定(10〜20個)はユーザーの手動判断が必要な部分。
  本計画では「候補出しの補助」までを開発タスクとし、最終リストの確定は
  別途相談する。

## フェーズ

### Phase 0: 環境構築
`uv init` 相当のプロジェクト初期化、Python 3.12+ 固定、ディレクトリ構成、
`.gitignore`(SQLiteファイル・.envなど)。

### Phase 1: 対象リポジトリ選定
SPEC.mdの選定基準(スター数/開始日/企業・個人)に沿って10〜20リポジトリを
確定し、SPEC.mdの表を更新する。

### Phase 2: GitHub APIクライアント
認証(PAT、環境変数経由)、レート制限を考慮した最小限のHTTPクライアント、
「指定パスが存在するか」を返す関数。

### Phase 3: チェックロジック
SPEC.mdの6項目それぞれについて、Phase 2のクライアントを使った判定関数を
実装する。

### Phase 4: 永続化
SQLiteスキーマ(`repo_checks`)を作成し、チェック結果を1リポジトリ1レコード
で保存する処理を実装する。

### Phase 5: 実行統合・出力
対象リポジトリ一覧を読み込み→チェック→保存までを1コマンドで実行できる
スクリプトにまとめ、さらに保存済みデータをMarkdown表かCSVで出力する
簡易ビューアを用意する(使用感確認のコア)。

### Phase 6: 手動実行・振り返り
実際に対象リポジトリへ実行し、結果を目視で確認。SPEC.mdのチェック項目や
対象リポジトリの妥当性を見直すインプットにする。

### Phase 7(MVP後): 開発方針(2026-09-08確定)

MVPの振り返り(#9)と品質評価(reports/mvp_data_insights_evaluation.md、
reports/api_cost_evaluation.md)を経て、データからの考察はClaude Project
の専用チャットスペースで行う方針とした(実装コンテキストの混入を避ける
ため。#12は中止、詳細はIssue.md参照)。このリポジトリ側の開発は「収集・
分析の質を上げること」「データを考察しやすい形で受け渡すこと」に集中
する。

**着手順**(ユーザー判断により#15を最優先、以降は依存関係に沿った順序)
1. **#15 LLMによるCLAUDE.md/AGENTS.md内容分析**: #5のルールベース分析
   (キーワード一致)の限界を、reports/agent_doc_analysis_validation.md
   の検証結果を踏まえて補う。
2. **#19 データをClaude Projectから参照しやすい公開形式の検討**: #12
   中止に伴い新設。Claude Project側での考察を再開するための前提条件。
3. **#10 GitHub Actions週次cron化**: #19で決めた公開形式を自動更新
   する。
4. **#13 対象リポジトリ追加・入れ替えのフロー整備**: 収集・公開の
   パイプラインが安定した後に対象拡大を検討する。

**バックログ**(優先度低、着手時期未定)
- **#11 レート制限・エラーハンドリングの強化**:
  reports/api_cost_evaluation.mdの実測で、現状規模(20リポジトリ・週次
  運用)ではレート制限消費が4.7%程度で余裕があると確認済み。#13で対象が
  100件超に拡大し、かつ#10でGitHub Actions既定トークンを使う場合にのみ
  再検討する。
- **#18 可観測性指標の測り方の再設計**: #16で廃止したhas_observability_dep
  の代替指標検討。そもそも復活が必要かどうかも含めて未確定。
- **#20 has_ci / agent_doc_has_code_blockの天井・床効果の見直し**:
  reports/mvp_data_insights_evaluation.mdで発見(新規)。両項目とも
  segment間の差がほとんどなく、比較材料として機能していない。

**リポジトリ公開**: SPEC.md/CLAUDE.mdはOSS公開を最終目標とするが、
現時点(2026-09-08)では非公開(reports/api_cost_evaluation.mdは誤って
公開済み前提で書かれていた点に注意)。公開タイミングは今回の計画には
含めず、非公開のまま運用を先に固める。

## リスク・懸念

- GitHub APIのレート制限(未認証60req/h、PAT利用で5000req/h)。対象が
  20リポジトリ×6項目程度ならPAT利用で問題にならない想定。
- `has_observability_dep` の判定精度(文字列一致では誤検知/見逃しがあり
  得る)。MVPでは精度より「まず動かして傾向を見る」を優先する。
- 対象リポジトリ選定の主観性。SPEC.mdに選定理由をメモしておき、後から
  見直せるようにする。
